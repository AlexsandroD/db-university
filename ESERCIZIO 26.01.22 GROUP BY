== 1 == 

SELECT COUNT(*),`enrolment_date`
FROM `students`
GROUP BY `enrolment_date`

== 2 == 

SELECT COUNT(*), `office_address`
FROM `teachers` 
GROUP BY `office_address`


== 3 == 

SELECT AVG(`exam_id`),`vote`
FROM `exam_student` 
GROUP by `vote`

== 4 ==
